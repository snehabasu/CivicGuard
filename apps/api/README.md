# API Skeleton

Planned responsibilities:
- Accept demo audio upload metadata
- Return transcript (mock in current phase)
- Generate case summary + SOAP + stress flags

Current status:
- Interface-only stubs, no external integrations
